import 'package:flutter/material.dart';

class DetalhesTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Detalhes da OS'),
    );
  }
}
